<h1><%= @playlist.name %>

  <ul>
    <% @playlist.songs.each do |song| %>
      <li><%= link_to song.title, song %></li>
    <% end %>
  </ul>
  <h4><%= link_to "Edit", edit_playlist_path(@playlist) %></h4>
